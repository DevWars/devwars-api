{
    "watch": ["**/*.ts", ".env"],
    "ext": "ts",
    "ignore": ["*.test.ts"],
    "exec": "node --inspect -r ts-node/register ./app/index.ts",
    "sourceMaps": true,
    "cwd": "${workspaceRoot}",
    "protocol": "inspector",
    "env": {
        "NODE_ENV": "development"
    }
}
